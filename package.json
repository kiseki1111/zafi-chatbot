{
  "name": "rbac-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "build:backend": "npm run build -w rbac-service",
    "build:web": "npm run build -w nextjs_tailwind_shadcn_ts",
    "prisma:generate": "npm run prisma:generate -w rbac-service"
  }
}
